// const btn = document.querySelector('button') //button
// const inputs = document.querySelector('form') //ement an array reprsent on fiels in array
// btn.addEventListener('click', () => { //button lisner

//     const name = (inputs.elements["name"].value).trim()
//     const email = (inputs.elements["email"].value).trim()
//     const msg = (inputs.elements["message"].value.trim())
//     const phone = (inputs.elements["phone"].value).trim()
//     if (!name.length > 0 || !email.length > 0 || !msg.length > 0 || !phone.length > 0) {
//         alert("All fields are mandatory")
//         return
//     }
//     Email.send({
//         Host: "smtp.mailtrap.io", //any email
//         Username: "50ca4e28db72c3",
//         Password: "1131bc7da98581", //details of person
//         To: "test@gmail.com", //actual email 
//         From: email.value,
//         Subject: "Contact Us Query By the Customer",
//         Body: name.value + "<br>" + phone.value +"<br>" + msg.value 
//     }).then(msg => alert("The email successfully sent"))
// })
